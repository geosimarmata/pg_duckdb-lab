{
  "name": "pg_duckdb-lab",
  "forwardPorts": [3000, 5432],
  "portsAttributes": {
    "3000": { "label": "Metabase", "onAutoForward": "openBrowser" },
    "5432": { "label": "Postgres" }
  },
  "postCreateCommand": "mkdir -p data/pgdata && sudo chown -R 999:999 data/pgdata && sudo chmod 700 data/pgdata && docker compose up -d"
}
